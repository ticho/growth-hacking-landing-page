<%= form_with model: User.new, class: 'form-inline' do |f| %>
  <%= f.email_field :email, class: 'form-control', placeholder: 'sevenstripes@gmail.com' %>
  <%= f.submit "S'abonner", class: 'btn btn-outline-secondary' %>
<% end %>